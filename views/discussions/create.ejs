<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | MangaVerse</title>
  <link rel="stylesheet" href="/css/style2.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Bowlby+One+SC&family=DM+Mono:ital,wght@0,300;0,400;0,500;1,400&display=swap" rel="stylesheet">
</head>
<body>
  <%- include('../partials/navbar') %>
  
  <main class="main-content">
    <%- include('../partials/flash') %>
    
    <div class="container narrow-container">
      
      <div class="discussion-top-bar centered-bar">
        <div class="header-pill">
          <h1><i class="fas fa-edit"></i> START NEW DISCUSSION</h1>
        </div>
      </div>

      <div class="create-terminal-card">
        
        <div class="terminal-header">
            <span class="dot red"></span>
            <span class="dot yellow"></span>
            <span class="dot green"></span>
            <span class="terminal-title">MangaVerse</span>
        </div>

        <form action="/discussions/create" method="POST" class="cyber-form">
          
          <div class="form-group">
            <label for="manga" class="cyber-label">
              <i class="fas fa-book-open"></i> Select Manga
            </label>
            <div class="select-wrapper">
                <select name="manga" id="manga" required class="cyber-input">
                  <option value="">-- Choose a manga --</option>
                  <% manga.forEach(m => { %>
                    <option value="<%= m._id %>"><%= m.title %></option>
                  <% }) %>
                </select>
                <i class="fas fa-chevron-down select-arrow"></i>
            </div>
          </div>

          <div class="form-group">
            <label for="title" class="cyber-label">
              <i class="fas fa-heading"></i> Discussion Title
            </label>
            <input 
              type="text" 
              id="title" 
              name="title" 
              required 
              maxlength="200"
              class="cyber-input"
              placeholder="What topic do you want to discuss?"
            >
          </div>

          <div class="form-group">
            <label for="content" class="cyber-label">
              <i class="fas fa-align-left"></i> Discussion Content
            </label>
            <textarea 
              id="content" 
              name="content" 
              required 
              rows="10"
              maxlength="5000"
              class="cyber-textarea"
              placeholder="Write your thoughts here..."
            ></textarea>
            <div class="form-hint">
                <span><i class="fas fa-info-circle"></i> Be respectful to others</span>
                <span>Max 5000 chars</span>
            </div>
          </div>

          <div class="form-actions-cyber">
            <a href="/discussions" class="btn btn-outline">
              Cancel
            </a>
            <button type="submit" class="btn btn-primary btn-large">
              POST DISCUSSION <i class="fas fa-paper-plane"></i>
            </button>
          </div>

        </form>
      </div>
    </div>
  </main>

  <%- include('../partials/footer') %>
  <script src="/js/main.js"></script>
</body>
</html>