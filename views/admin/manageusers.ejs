<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Manage Users | Admin Console</title>
  <link rel="stylesheet" href="/css/style2.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=DM+Mono:ital,wght@0,300;0,400;0,500;1,400&display=swap" rel="stylesheet">
  
  <style>
    /* Admin Table Styles */
    .user-list-card {
        background: var(--card-bg);
        border: 1px solid rgba(255,255,255,0.05);
        border-radius: 20px;
        overflow: hidden;
        margin-top: 2rem;
    }
    
    .cyber-table {
        width: 100%;
        border-collapse: collapse;
        font-family: "DM Mono", monospace;
    }
    
    .cyber-table th {
        background: #000;
        color: var(--brand-blue);
        text-align: left;
        padding: 1rem 1.5rem;
        border-bottom: 2px solid rgba(255,255,255,0.1);
        font-size: 0.9rem;
        text-transform: uppercase;
    }
    
    .cyber-table td {
        padding: 1rem 1.5rem;
        border-bottom: 1px solid rgba(255,255,255,0.05);
        color: var(--text-main);
    }
    
    .role-badge {
        padding: 2px 8px;
        border-radius: 4px;
        font-size: 0.8rem;
        font-weight: bold;
        text-transform: uppercase;
    }
    .role-admin { color: #ff4444; border: 1px solid #ff4444; box-shadow: 0 0 5px rgba(255,68,68,0.2); }
    .role-user { color: var(--text-muted); border: 1px solid var(--text-muted); }
    
    .action-btn {
        background: none; border: none; cursor: pointer;
        font-family: "DM Mono"; font-weight: bold;
        padding: 5px 10px; border-radius: 4px; transition: 0.3s;
    }
    .btn-promote { border: 1px solid #27c93f; color: #27c93f; }
    .btn-promote:hover { background: #27c93f; color: #000; box-shadow: 0 0 10px #27c93f; }
    
    .btn-demote { border: 1px solid #ff4444; color: #ff4444; }
    .btn-demote:hover { background: #ff4444; color: #fff; box-shadow: 0 0 10px #ff4444; }

    /* DANGER ZONE (Delete Manga Section) */
    .danger-zone-card {
        background: rgba(255, 68, 68, 0.05); /* Slight red tint */
        border: 1px solid #ff4444;
        border-radius: 20px;
        padding: 2rem;
        margin-top: 3rem;
        margin-bottom: 3rem;
    }
    .danger-header {
        display: flex; align-items: center; gap: 10px; color: #ff4444;
        margin-bottom: 1.5rem; border-bottom: 1px solid rgba(255, 68, 68, 0.2);
        padding-bottom: 1rem;
    }
    .delete-form {
        display: flex; gap: 1rem; align-items: center; flex-wrap: wrap;
    }
    .cyber-select {
        background: #000; color: #fff; border: 1px solid #333;
        padding: 0.8rem; border-radius: 8px; font-family: "DM Mono";
        min-width: 300px;
    }
    .btn-danger-solid {
        background: #ff4444; color: #fff; border: none; padding: 0.8rem 1.5rem;
        border-radius: 8px; cursor: pointer; font-weight: bold;
        transition: 0.3s; display: flex; align-items: center; gap: 8px;
    }
    .btn-danger-solid:hover { background: #cc0000; box-shadow: 0 0 15px rgba(255,68,68,0.4); }
  </style>
</head>
<body>
  <%- include('../partials/navbar') %>
  
  <main class="main-content">
    <%- include('../partials/flash') %>
    
    <div class="container">
       <div class="discussion-top-bar centered-bar">
        <div class="header-pill admin-pill">
          <h1><i class="fas fa-users-cog"></i> USER PERMISSIONS PROTOCOL</h1>
        </div>
      </div>

      <div class="user-list-card">
        <table class="cyber-table">
            <thead>
                <tr>
                    <th>Username</th>
                    <th>Email</th>
                    <th>Current Role</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <% users.forEach(user => { %>
                    <tr>
                        <td>
                            <i class="fas fa-user-circle"></i> <%= user.username %>
                            <% if(user.username === 'bhavi12z') { %> <i class="fas fa-crown" style="color: gold;"></i> <% } %>
                        </td>
                        <td><%= user.email %></td>
                        <td>
                            <span class="role-badge <%= user.role === 'admin' ? 'role-admin' : 'role-user' %>">
                                <%= user.role %>
                            </span>
                        </td>
                        <td>
                            <% if (user.username !== 'bhavi12z') { %>
                                
                                <% if (user.role === 'admin') { %>
                                    <form action="/admin/demote/<%= user._id %>" method="POST" style="display:inline;">
                                        <button class="action-btn btn-demote">
                                            <i class="fas fa-arrow-down"></i> REVOKE
                                        </button>
                                    </form>
                                <% } else { %>
                                    <form action="/admin/promote/<%= user._id %>" method="POST" style="display:inline;">
                                        <button class="action-btn btn-promote">
                                            <i class="fas fa-arrow-up"></i> PROMOTE
                                        </button>
                                    </form>
                                <% } %>

                            <% } else { %>
                                <span style="color:var(--text-muted); font-size: 0.8rem;">[SUPER_USER]</span>
                            <% } %>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
      </div>
      

      <div style="text-align: center; margin-top: 2rem;">
        <a href="/admin/addManga" class="btn btn-outline">Go to Add Manga</a>
      </div>
      
    </div>
  </main>
  
  <%- include('../partials/footer') %>
</body>
</html>